--- a/src/genode/component.cc
+++ b/src/genode/component.cc
@@ -55,7 +55,6 @@
 #include "timeout_late.h"
 #include "gui.h"
 #include "audio.h"
-#include "xhci.h"
 #include "file.h"
 
 
@@ -1098,8 +1097,6 @@
 		Rtc::Session          *_rtc          { nullptr };
 		Seoul::Audio          *_audio        { nullptr };
 
-		Genode::Constructible<Seoul::Xhci> _xhci { };
-
 		using Vcpus_active = Genode::Bit_array<64>;
 
 		Vcpu *                 _vcpus[16]    { nullptr };
@@ -1588,15 +1585,6 @@
 				if (verbose)
 					Genode::log("device: ", name);
 
-				if (name == "xhci") {
-					if (_xhci.constructed()) {
-						Logging::panic("xhci can only by instantiated once");
-						throw Config_error();
-					}
-
-					_xhci.construct(_vmm.env, _vmm.heap, config, _motherboard);
-				}
-
 				Device_model_info *dmi = device_model_registry()->lookup(name.string());
 
 				if (!dmi) {
--- a/src/genode/device_model_registry.cc
+++ b/src/genode/device_model_registry.cc
@@ -90,8 +90,6 @@
 MODEL_INFO(virtio_fs,    "mem", "bdf", "msix", "verbose")
 MODEL_INFO(virtio_net,   "mem", "bdf", "msix", "verbose")
 
-MODEL_INFO(xhci, "mem")
-
 MODEL_INFO(ide, "port0", "port1", "irq", "bdf", "disk")
 MODEL_INFO(ahci, "mem", "bdf")
 MODEL_INFO(drive, "sigma0drive", "controller", "port", "verbose")
