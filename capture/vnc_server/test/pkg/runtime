runtime | ram: 250M | caps: 2000 | binary: init
+ requires
  + capture
  + event
  + gui
  + nic vnc
  | + tcp-forward | port: 5900 | domain: default | to: 10.0.10.2
  + fs font
  + fs model
  + fs target
  + fs report
  + report shape
  + report clipboard
  + rom clipboard
  + rom vimrc
  + rom mesa_gpu.lib.so
  + timer
+ content
  + rom passwd
+ config | verbose: yes
  + parent-provides
  | + service ROM
  | + service LOG
  | + service RM
  | + service CPU
  | + service PD
  | + service Gui
  | + service Timer
  | + service Capture
  | + service Event
  | + service Report
  | + service Nic
  | + service File_system
  + default | caps: 300
  + start vnc_server | caps: 1000 | ram: 130M
  | + route
  |   + service ROM | label: config
  |   | + parent | label: vnc.config
  |   + service Nic
  |   | + parent | label: vnc
  |   + any-service
  |     + parent
  |     + any-child
  + start system_shell | ram: 200M | caps: 1000
    + binary init
    + route
      + service ROM | label: config
      | + parent | label: system_shell.config
      + service ROM | label: vimrc
      | + parent | label: vimrc
      + service ROM | label: clipboard
      | + parent | label: clipboard
      + service Report | label: clipboard
      | + parent | label: clipboard
      + service File_system | label_prefix: font ->
      | + parent | identity: font
      + service File_system | label_prefix: target ->
      | + parent | identity: target
      + service File_system | label_prefix: model ->
      | + parent | identity: model
      + service File_system | label_prefix: report ->
      | + parent | identity: report
      + any-service
        + parent
        + any-child
-
