+++ src/src/init.cc
@@ -0,0 +1,97 @@
+/*
+ * \brief  pcsc-lite initialization
+ * \author Christian Prochaska
+ * \date   2016-10-11
+ */
+
+/*
+ * Copyright (C) 2016-2017 Genode Labs GmbH
+ *
+ * This file is part of the Genode OS framework, which is distributed
+ * under the terms of the GNU Affero General Public License version 3.
+ */
+
+/* libc includes */
+#include <fcntl.h>
+#include <stdio.h>
+#include <stdlib.h>
+#include <unistd.h>
+
+/* libusb includes */
+#include <libusb.h>
+
+/* pcsc-lite includes */
+extern "C" {
+#include <debuglog.h>
+#define _Atomic
+#include <readerfactory.h>
+}
+
+static constexpr bool verbose = false;
+
+struct Pcsc_lite_initializer
+{
+	LONG rv { 0 };
+
+	Pcsc_lite_initializer()
+	{
+		if (verbose) {
+			/* pcscd -f */
+			DebugLogSetLogType(DEBUGLOG_STDOUT_DEBUG);
+			/* pcscd -d */
+			DebugLogSetLevel(PCSC_LOG_DEBUG);
+			/* pcscd -a */
+			(void)DebugLogSetCategory(DEBUG_CATEGORY_APDU);
+		}
+
+		/*
+		 * Find out vendor id and product id of the connected USB device
+		 * and add it as reader.
+		 */
+
+		libusb_context *ctx = nullptr;
+
+		libusb_init(&ctx);
+
+		libusb_device **devs = nullptr;
+
+		if (libusb_get_device_list(ctx, &devs) < 1) {
+			fprintf(stderr, "Error: could not find a USB device\n");
+			exit(1);
+		}
+
+		struct libusb_device_descriptor device_descriptor;
+
+		if (libusb_get_device_descriptor(devs[0], &device_descriptor) < 0) {
+			fprintf(stderr, "Error: could not read the device descriptor "
+			                "of the USB device\n");
+			exit(1);
+		}
+
+		libusb_free_device_list(devs, 1);
+
+		libusb_exit(ctx);
+
+		char device[14];
+
+		snprintf(device, sizeof(device), "usb:%04x/%04x",
+		         device_descriptor.idVendor, device_descriptor.idProduct);
+
+		RFAllocateReaderSpace(0);
+
+		rv = RFAddReader("CCID", 0, "/", device);
+		if (rv)
+			fprintf(stderr, "Could not add reader: %x:%x %s\n",
+			        device_descriptor.idVendor, device_descriptor.idProduct,
+			        rv == SCARD_E_UNKNOWN_READER ?
+			        "(SCARD_E_UNKNOWN_READER) device is not present in Info.plist?"
+			        : "");
+	}
+};
+
+
+extern "C" LONG initialize_pcsc_lite()
+{
+	static Pcsc_lite_initializer pcsc_lite_initializer;
+	return pcsc_lite_initializer.rv;
+}
